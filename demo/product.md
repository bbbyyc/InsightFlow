# InsightFlow 产品能力

Web 工作台支持上传、查看和删除文档，展示解析、切分、Embedding 与入库进度，以及失败原因和重新执行入口。

用户可以创建会话、选择目标文档并继续历史对话。Agent 以 Plan、Search、Rewrite、Generate 节点运行，通过 SSE 返回节点状态和回答。回答引用使用 `[N]` 编号，用户可以打开对应文档、章节、Chunk 内容和页码位置。

评测页面只读取持久化的正式评测运行。没有经过人工审核的 Gold Label 时，页面显示“暂无已验证结果”，不会把合成 Fixture 的数字当成项目效果。
